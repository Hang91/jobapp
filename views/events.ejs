<%- include('includes/header') %>
<%- messages() %>
<div>
		<ul class="events">
		  <% 
		  for(index in events) {%>
		    <li class="events">
		      <span><%= events[index].name %></span>
		      <span><%= events[index].email %></span>
		      <% 
		      	for(eventsIndex in events[index].events){   %>
					<h1><%=events[index].events[eventsIndex].name%></h1>
		      <% } %>

		      %>
		    </li>
		  <% } %>
		</ul>
    
</div> 
    	<script>
			//document.getElementById("demo").innerHTML = events.name;

		</script>
    
      
        
<%- include('includes/footer') %>